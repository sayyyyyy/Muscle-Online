# import models

# def add_data():

#     user = User(name=name, password=hashlib.sha256(password.encode('utf-8')).hexdigest(), email=email)
#     models.db.session.add(user)
#     models.db.session.commit()
#     pass